<Page
    x:Class="SoSmartTv.VideoPlayer.Views.VideoDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoSmartTv.VideoPlayer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="Black">
        <Image VerticalAlignment="Center" HorizontalAlignment="Stretch" Opacity="0.2"
               Source="{x:Bind ViewModel.Details.BackdropPath}" />
        <Grid Margin="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="{x:Bind ViewModel.Details.PosterSource}"></Image>
            <TextBlock Grid.Column="1" TextWrapping="WrapWholeWords" Foreground="White" Text="{x:Bind ViewModel.Details.Description}"></TextBlock>
        </Grid>
    </Grid>
</Page>
