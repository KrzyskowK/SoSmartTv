<Page
    x:Class="SoSmartTv.VideoPlayer.Views.VideoDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoSmartTv.VideoPlayer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="Black">
        <Image VerticalAlignment="Center" HorizontalAlignment="Stretch" Opacity="0.2"
               Source="{Binding Details.BackdropPath}" />
        <StackPanel>
            <TextBlock Margin="50,30" Foreground="White" Text="{Binding Details.Title}" FontSize="40" ></TextBlock>
            <Grid Margin="50" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="{Binding Details.PosterSource}" VerticalAlignment="Top"></Image>
                <TextBlock Grid.Column="1" Padding="20" TextWrapping="WrapWholeWords" Foreground="White" Text="{Binding Details.Description}"></TextBlock>
            </Grid>
        </StackPanel>
        
    </Grid>
</Page>
